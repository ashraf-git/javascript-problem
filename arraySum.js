var numbers = [24, 58, 55, 85, 58, 57, 24, 54];
var sum = 0;
for (let i = 0; i < numbers.length; i++) {
    var element = numbers[i];
     sum = sum + element;
}
console.log('total of the numbers:', sum);